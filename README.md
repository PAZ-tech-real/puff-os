## puff OS a os for x86

puff os is a 16 bit OS runing on x86 computers

## notes/warnings:

os needs to be on a floopy disc (old disk esaly brakes)
its runs on bios (becoming obsolte)
runs 16 bit (slow for modern computing)
is in text mode (no games)
limted comands (echo input, and read)
64 carter limt in buffer (no lage text in buffer)
